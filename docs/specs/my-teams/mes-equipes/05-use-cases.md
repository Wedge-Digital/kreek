# Phase 5 — Use cases — page "Mes équipes"

Aucune mutation sur cette page : les deux endpoints (`my_teams`,
`my_teams_widget`) sont des `GET` de lecture seule. Chaque carte (brouillon,
active, archivée) est un lien de navigation HTMX vers une autre page (build
de brouillon ou détail d'équipe existante) — aucune commande, aucun use
case, aucune erreur applicative à définir pour cette page.

Phase volontairement vide, tracée pour cohérence avec le reste du workflow.
