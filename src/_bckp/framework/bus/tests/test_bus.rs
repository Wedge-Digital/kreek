#[test]
pub fn assert_instanciate_message_bus() {
    // let mw1 = TestMiddleware::new();
    // let mw2 = TestMiddleware::new();
    // let mw3 = TestMiddleware::new();
    // let mut mw_chain: Vec<Box<dyn Middleware<Command>>> = vec![Box::new(mw1), Box::new(mw2), Box::new(mw3)];
    // let bus = Bus::new(&mut mw_chain);
    // bus.add_middleware(Box::new(TestMiddleware::new()));
    // bus.add_middleware(Box::new(TestMiddleware::new()));
    // bus.add_middleware(Box::new(TestMiddleware::new()));
    // assert_eq!(bus.mw_count(), 3);
}