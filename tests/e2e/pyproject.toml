[project]
name = "e2e"
version = "0.1.0"
description = "Tests end-to-end (Playwright) du serveur kreek"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "pytest-playwright>=0.5",
]
